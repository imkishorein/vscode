/*--------------------------------------------------------------------------------------------- *  Copyright (c) Microsoft Corporation. All rights reserved. *  Licensed under the MIT License. See License.txt in the project root for license information. *--------------------------------------------------------------------------------------------*//** * PreviewEditorPanel - Renders preview in the main editor panel * * Naming Convention: * - Component: PreviewEditorPanel (conceptual name for editor panel rendering) * - Classes: PreviewEditor, PreviewEditorInput * - Trigger: Click from PreviewPrimarySidebarView, PreviewExplorerView, or "Preview" mode in ViewModeDropdownControl * - Location: Main editor area * - Mode: 'preview-only' */import './media/previewEditor.css';import { Dimension, getWindow } from '../../../../base/browser/dom.js';import { CancellationToken } from '../../../../base/common/cancellation.js';import { URI } from '../../../../base/common/uri.js';import { IEditorOptions } from '../../../../platform/editor/common/editor.js';import { IFileService } from '../../../../platform/files/common/files.js';import { IStorageService } from '../../../../platform/storage/common/storage.js';import { ITelemetryService } from '../../../../platform/telemetry/common/telemetry.js';import { IThemeService } from '../../../../platform/theme/common/themeService.js';import { EditorPane } from '../../../browser/parts/editor/editorPane.js';import { IEditorOpenContext, IUntypedEditorInput } from '../../../common/editor.js';import { EditorInput } from '../../../common/editor/editorInput.js';import { IEditorGroup } from '../../../services/editor/common/editorGroupsService.js';import { IWebviewService, IWebviewElement } from '../../webview/browser/webview.js';import { ITextFileService, ITextFileSaveEvent, ITextFileEditorModel, snapshotToString } from '../../../services/textfile/common/textfiles.js';import { IDisposable } from '../../../../base/common/lifecycle.js';export interface IPreviewEditorOptions extends IEditorOptions {	readonly previewTitle?: string;}export class PreviewEditorInput extends EditorInput {	static readonly ID = 'workbench.input.previewEditor';	constructor(		public readonly previewTitle: string	) {		super();	}	override get typeId(): string {		return PreviewEditorInput.ID;	}	override getName(): string {		return this.previewTitle;	}	override get resource(): URI | undefined {		return URI.from({ scheme: 'preview', path: this.previewTitle });	}	override matches(otherInput: EditorInput | IUntypedEditorInput): boolean {		if (otherInput instanceof PreviewEditorInput) {			return otherInput.previewTitle === this.previewTitle;		}		return false;	}}export class PreviewEditor extends EditorPane {	static readonly ID = 'workbench.editor.previewEditor';	private rootElement: HTMLElement | undefined;	private toolbar: HTMLElement | undefined;	private contentWrapper: HTMLElement | undefined;	private currentPreviewNumber: string | undefined;	private currentHtmlPath: URI | undefined;	private webview: IWebviewElement | undefined;	private modelListener: IDisposable | undefined;	private webviewDisposable: IDisposable | undefined;	constructor(		group: IEditorGroup,		@ITelemetryService telemetryService: ITelemetryService,		@IThemeService themeService: IThemeService,		@IStorageService storageService: IStorageService,		@IFileService private readonly fileService: IFileService,		@IWebviewService private readonly webviewService: IWebviewService,		@ITextFileService private readonly textFileService: ITextFileService	) {		super(PreviewEditor.ID, group, telemetryService, themeService, storageService);		// Watch for file changes on disk		this._register(this.fileService.onDidFilesChange(event => {			if (this.currentPreviewNumber) {				const htmlPath = URI.file(`/Users/kishore.v/Dev/vscode/.preview-samples/preview${this.currentPreviewNumber}.html`);				// Check if the current HTML file was updated				if (event.contains(htmlPath)) {					this.render(this.input instanceof PreviewEditorInput ? this.input.previewTitle : '');				}			}		}));		// Watch for file saves in the editor (covers save from outside text model listener)		this._register(this.textFileService.files.onDidSave((e: ITextFileSaveEvent) => {			// Check if the saved file matches the current preview			if (this.currentPreviewNumber) {				const expectedPath = `/Users/kishore.v/Dev/vscode/.preview-samples/preview${this.currentPreviewNumber}.html`;				if (e.model.resource.fsPath === expectedPath) {					this.render(this.input instanceof PreviewEditorInput ? this.input.previewTitle : '');				}			}		}));	}	override async setInput(input: EditorInput, options: IEditorOptions | undefined, context: IEditorOpenContext, token: CancellationToken): Promise<void> {		await super.setInput(input, options, context, token);		if (input instanceof PreviewEditorInput) {			// Extract preview number from title (e.g., "Preview 1" -> "1")			const match = input.previewTitle.match(/\d+/);			this.currentPreviewNumber = match ? match[0] : undefined;			this.render(input.previewTitle);		}	}	protected createEditor(parent: HTMLElement): void {		// Create root element		this.rootElement = document.createElement('div');		this.rootElement.classList.add('preview-editor');		parent.appendChild(this.rootElement);		// Create toolbar		this.toolbar = document.createElement('div');		this.toolbar.classList.add('preview-toolbar');		// Desktop button		const desktopButton = document.createElement('button');		desktopButton.classList.add('preview-toolbar-button', 'preview-toolbar-button-active');		desktopButton.textContent = 'Desktop';		desktopButton.setAttribute('aria-label', 'Desktop view');		this.toolbar.appendChild(desktopButton);		// Mobile button		const mobileButton = document.createElement('button');		mobileButton.classList.add('preview-toolbar-button');		mobileButton.textContent = 'Mobile';		mobileButton.setAttribute('aria-label', 'Mobile view');		this.toolbar.appendChild(mobileButton);		// New Window button		const newWindowButton = document.createElement('button');		newWindowButton.classList.add('preview-toolbar-button');		newWindowButton.textContent = 'New Window';		newWindowButton.setAttribute('aria-label', 'Open in new window');		this.toolbar.appendChild(newWindowButton);		this.rootElement.appendChild(this.toolbar);		// Create content wrapper for webview		this.contentWrapper = document.createElement('div');		this.contentWrapper.classList.add('preview-content-wrapper');		this.rootElement.appendChild(this.contentWrapper);	}	private async render(title: string): Promise<void> {		if (!this.contentWrapper) {			return;		}		// Dispose existing webview to ensure clean state		if (this.webview) {			this.webviewDisposable?.dispose();			this.webview = undefined;			this.webviewDisposable = undefined;		}		// Clear content wrapper		while (this.contentWrapper.firstChild) {			this.contentWrapper.removeChild(this.contentWrapper.firstChild);		}		// Load the HTML file content		if (this.currentPreviewNumber) {			const htmlPath = URI.file(`/Users/kishore.v/Dev/vscode/.preview-samples/preview${this.currentPreviewNumber}.html`);			this.currentHtmlPath = htmlPath;			try {				const model = await this.textFileService.files.resolve(htmlPath);				const htmlContent = this.getModelContent(model);				// Create webview with proper container state				this.webview = this.webviewService.createWebviewElement({					title: title,					options: {},					contentOptions: {						allowScripts: true,						allowForms: true,						localResourceRoots: []					},					extension: undefined				});				this.webviewDisposable = this._register(this.webview);				this.webview.mountTo(this.contentWrapper, getWindow(this.contentWrapper));				this.webview.setHtml(htmlContent);				this.registerModelListener(model, title, htmlPath);			} catch (error) {				try {					const fileContent = await this.fileService.readFile(htmlPath);					const htmlContent = fileContent.value.toString();					// Create webview with proper container state					this.webview = this.webviewService.createWebviewElement({						title: title,						options: {},						contentOptions: {							allowScripts: true,							allowForms: true,							localResourceRoots: []						},						extension: undefined					});					this.webviewDisposable = this._register(this.webview);					this.webview.mountTo(this.contentWrapper, getWindow(this.contentWrapper));					this.webview.setHtml(htmlContent);				} catch (readError) {					// Show error message if file cannot be read					const errorDiv = document.createElement('div');					errorDiv.textContent = `Error loading preview: ${readError}`;					errorDiv.style.color = 'var(--vscode-errorForeground)';					errorDiv.style.padding = '20px';					this.contentWrapper.appendChild(errorDiv);				}			}		}	}	private registerModelListener(model: ITextFileEditorModel, title: string, resource: URI): void {		this.modelListener?.dispose();		const listener = model.onDidChangeContent(() => {			if (this.currentHtmlPath && this.currentHtmlPath.fsPath === resource.fsPath) {				const htmlContent = this.getModelContent(model);				this.updateWebviewContent(htmlContent, title);			}		});		this.modelListener = this._register(listener);	}	private getModelContent(model: ITextFileEditorModel): string {		const textEditorModel = model.textEditorModel;		if (textEditorModel) {			return textEditorModel.getValue();		}		const snapshot = model.createSnapshot();		return snapshot ? snapshotToString(snapshot) : '';	}	private updateWebviewContent(htmlContent: string, title: string): void {		if (!this.webview) {			return;		}		// Update HTML content without recreating webview		this.webview.setHtml(htmlContent);	}	override layout(dimension: Dimension): void {		if (this.webview) {			// Webview handles its own layout		}	}	override focus(): void {		if (this.webview) {			this.webview.focus();		}	}	override clearInput(): void {		this.modelListener?.dispose();		this.modelListener = undefined;		this.webviewDisposable?.dispose();		this.webviewDisposable = undefined;		this.webview = undefined;		if (this.contentWrapper) {			while (this.contentWrapper.firstChild) {				this.contentWrapper.removeChild(this.contentWrapper.firstChild);			}		}		super.clearInput();	}}