/*--------------------------------------------------------------------------------------------- *  Copyright (c) Microsoft Corporation. All rights reserved. *  Licensed under the MIT License. See License.txt in the project root for license information. *--------------------------------------------------------------------------------------------*/import { localize, localize2 } from '../../../../nls.js';import { Action2, MenuId, registerAction2 } from '../../../../platform/actions/common/actions.js';import { ServicesAccessor } from '../../../../platform/instantiation/common/instantiation.js';import { IQuickInputService, IQuickPickItem } from '../../../../platform/quickinput/common/quickInput.js';import { Codicon } from '../../../../base/common/codicons.js';import { IViewModeManager } from './viewModeManager.js';import { IWorkbenchLayoutService, Parts } from '../../../services/layout/browser/layoutService.js';interface IViewModeQuickPickItem extends IQuickPickItem {	mode: 'code-only' | 'preview-only' | 'split-view';}/** * Action that shows a quick pick to select view mode */class PreviewModeQuickPickAction extends Action2 {	constructor() {		super({			id: 'workbench.action.previewModeQuickPick',			title: localize2('selectViewMode', 'Select View Mode'),			icon: Codicon.play,			menu: [{				id: MenuId.EditorTitle,				group: 'navigation',				order: -2			}],			f1: false		});	}	async run(accessor: ServicesAccessor): Promise<void> {		const quickInputService = accessor.get(IQuickInputService);		const viewModeManager = accessor.get(IViewModeManager);		const layoutService = accessor.get(IWorkbenchLayoutService);		const currentMode = viewModeManager.currentMode;		const items: IViewModeQuickPickItem[] = [			{				label: '$(code) Code',				description: 'Show only code',				mode: 'code-only',				picked: currentMode === 'code-only'			},			{				label: '$(preview) Preview',				description: 'Show only preview',				mode: 'preview-only',				picked: currentMode === 'preview-only'			},			{				label: '$(split-horizontal) Code + Preview',				description: 'Show code and preview side-by-side',				mode: 'split-view',				picked: currentMode === 'split-view'			}		];		const selected = await quickInputService.pick(items, {			placeHolder: localize('selectViewModePlaceholder', 'Select view mode'),			canPickMany: false		});		if (selected) {			console.log('[PreviewMode] Selected mode:', selected.mode);			if (selected.mode === 'split-view') {				layoutService.setPartHidden(false, Parts.AUXILIARYBAR_PART);			} else {				layoutService.setPartHidden(true, Parts.AUXILIARYBAR_PART);			}			viewModeManager.setMode(selected.mode);		}	}}registerAction2(PreviewModeQuickPickAction);